eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  data:
    mongodb:
      host: localhost
  cloud:
    stream:
      kafka:
        binder:
          zkNodes: localhost
          brokers: localhost